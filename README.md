# Детектус (Detectus)

Сервис для проверки медиафайлов на наличие сгенерированного контента

## Описание

Detectus - это веб-приложение для анализа изображений на предмет AI-генерации и deepfake-манипуляций. Сервис использует платформу Sightengine для детекции искусственного контента через автоматизированный Selenium-браузер.

## Возможности

- 📁 **Загрузка изображений** через drag & drop или выбор файла
- 🤖 **Анализ AI-генерации** - определение сгенерированных нейросетями изображений
- 🎭 **Deepfake детекция** - выявление манипуляций с лицами
- 📊 **Детальная статистика** - проценты вероятности по разным категориям
- 🏷️ **Идентификация моделей** - определение конкретных diffusion и GAN моделей
- 📚 **История анализов** - сохранение и просмотр предыдущих проверок
- 📱 **Адаптивный интерфейс** - работа на desktop и мобильных устройствах

## Технологии

### Бэкенд
- **FastAPI** - асинхронный веб-фреймворк
- **Selenium** - автоматизация браузера для работы с Sightengine
- **ChromeDriver** - управление браузером Chrome
- **UUID** - генерация уникальных идентификаторов файлов
- **JSONL** - хранение истории анализов

### Фронтенд
- **Vanilla JavaScript** - клиентская логика
- **CSS Grid/Flexbox** - адаптивная верстка
- **Fetch API** - взаимодействие с бэкендом
- **Drag & Drop API** - удобная загрузка файлов

## Установка и запуск

### Предварительные требования

- Python 3.8+
- Chrome/Chromium браузер
- Аккаунт на [Sightengine](https://sightengine.com/)

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd detectus
```

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 3. Настройка окружения

Создайте файл `.env` в корне проекта:

```env
SIGHTENGINE_EMAIL=your_email@sightengine.com
SIGHTENGINE_PASSWORD=your_password
```

### 4. Запуск приложения

```bash
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

Приложение будет доступно по адресу: `http://localhost:8000`

## Структура проекта

```
detectus/
├── main.py                 # Основное FastAPI приложение
├── requirements.txt        # Зависимости Python
├── templates/
│   └── index.html         # Главный HTML шаблон
├── static/
│   ├── style.css          # Стили приложения
│   └── app.js            # Клиентский JavaScript
├── uploads/               # Директория для загруженных файлов
├── history.jsonl          # Файл истории анализов
└── README.md             # Документация
```

## API Endpoints

### `POST /analyze`
Анализ загруженного изображения

**Параметры:**
- `file`: файл изображения (multipart/form-data)

**Ответ:**
```json
{
  "id": "уникальный_идентификатор",
  "image_url": "/uploads/файл.jpg",
  "summary": "Likely AI-generated",
  "score": "85%",
  "genai_percent": "85%",
  "deepfake_percent": "12%",
  "raw_text": "полный_текст_результата"
}
```

### `GET /history`
Получение истории анализов

**Параметры:**
- `limit`: количество записей (по умолчанию 10)

### `GET /`
Главная страница приложения

## Использование

1. **Загрузка файла** - перетащите изображение в область загрузки или выберите файл вручную
2. **Запуск анализа** - нажмите кнопку "Отправить на анализ"
3. **Просмотр результатов** - изучите детальную статистику в правой колонке
4. **История** - возвращайтесь к предыдущим анализам через нижнюю панель

## Особенности реализации

- **Автоматическая авторизация** на Sightengine через Selenium
- **Обработка больших файлов** с прогресс-баром
- **Валидация форматов** поддерживаемых изображений
- **Обработка ошибок** с понятными сообщениями пользователю
- **Headless режим** для работы на сервере

## Безопасность

- Пароли хранятся в переменных окружения
- Уникальные имена файлов предотвращают конфликты
- Валидация MIME-типов загружаемых файлов

## Разработка

Для разработки с включенным браузером:

```python
# В main.py закомментируйте строки:
# options.add_argument("--headless=new")
```

## Лицензия

[Указать лицензию]

## Поддержка

По вопросам работы сервиса обращайтесь: [контактная информация]
